/* exported getFirstChar */
function getFirstChar(string) {
  return string[0];
}

// define a function named getFirstChar with a single paramater, string
// return the string at index 0, the first character
