/* exported isLowerCased */
function isLowerCased(word) {
  var lowerCaseWord = word.toLowerCase();
  if (word === lowerCaseWord) {
    return true;
  } else {
    return false;
  }
}

// define a function named isLowerCased with one parameter, word
// define a variable named lowerCase word which is assigned the value of the word with the toLoerCase method
// use an if condition to evaluate if the lowerCaseWord is strictly equal to word
// return true if true, else return false
