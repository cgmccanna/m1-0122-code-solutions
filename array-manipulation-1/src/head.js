/* exported head */
function head(array) {
  var headValue = array[0];
  return headValue;
}

// define a fucnction named head, which has a single parameter 'array'
// define a variable headValue and assign it the value of array at 0
// return the value of headValue, the first element in the array
